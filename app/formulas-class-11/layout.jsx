export default function FormulasLayout({ children }) {
    return (
        <div className="min-h-screen bg-gradient-to-br from-cyan-100 via-cyan-50 to-teal-100">
            {children}
        </div>
    );
}
